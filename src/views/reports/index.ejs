<section class="page-header">
  <div>
    <p class="eyebrow">Reporting · P5-C</p>
    <h1>报告中心</h1>
    <p class="muted"><%= project.name %> · <%= project.primaryDomain %></p>
  </div>
  <div class="actions">
    <form action="/projects/<%= project.id %>/reports" method="post"><button class="button" type="submit">生成项目报告</button></form>
  </div>
</section>

<section class="panel">
  <h2>可复现快照</h2>
  <p class="muted">PROJECT_REPORT_V1 只聚合已保存的 SEO、GEO、Content 与 Competitor 数据。确定性事实与 AI 建议分开保存；生成报告本身不会调用 DeepSeek，也不会产生 P6 AI Visibility / Share of Voice 数据。</p>
</section>

<section class="metric-grid compact">
  <article class="metric-card"><div class="metric-title">报告快照</div><div class="metric-value"><%= reports.length %></div><div class="metric-caption">历史可追溯</div></article>
  <article class="metric-card"><div class="metric-title">当前版本</div><div class="metric-value">V1</div><div class="metric-caption">PROJECT_REPORT_V1</div></article>
  <article class="metric-card"><div class="metric-title">导出</div><div class="metric-value">JSON</div><div class="metric-caption">原始快照</div></article>
</section>

<section class="panel">
  <div class="panel-header"><div><h2>报告历史</h2><p class="muted">每次生成都会创建新的不可变快照。</p></div></div>
  <div class="table-wrap"><table><thead><tr><th>版本</th><th>类型</th><th>生成时间</th><th>Executive AI</th><th>操作</th></tr></thead><tbody>
  <% for (const report of reports) { %>
    <tr>
      <td><strong><%= report.reportVersion %></strong></td>
      <td><%= report.reportType %></td>
      <td><%= new Date(report.createdAt).toLocaleString('zh-CN') %></td>
      <td><%= report.executiveAiTaskId ? '已关联' : '未生成' %></td>
      <td><a href="/projects/<%= project.id %>/reports/<%= report.id %>">查看报告</a></td>
    </tr>
  <% } %>
  <% if (!reports.length) { %><tr><td colspan="5" class="muted">尚无报告。点击“生成项目报告”创建第一份确定性快照。</td></tr><% } %>
  </tbody></table></div>
</section>
