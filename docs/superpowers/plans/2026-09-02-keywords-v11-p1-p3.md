# Keywords V1.1 P1-P3 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Close V1.1 P1-P3 by incrementally upgrading the existing keyword subsystem with bulk management, lifecycle, Cluster semantics, and explainable opportunity scoring.

**Architecture:** Preserve existing keyword tables, routes, services, authorization, UI, and evidence projections. Add only backward-compatible schema, focused domain modules, validated commands, and workbench controls. Scores are append-only snapshots produced by a pure evidence-aware engine.

**Tech Stack:** Node.js 22, TypeScript, Express 5, Prisma/PostgreSQL, Zod, EJS, Vitest, Playwright.

**Spec:** `docs/superpowers/specs/2026-09-02-keywords-v11-p1-p3-design.md`

## Global Constraints

- Start from exact SHA `461193813cb5dc61e1d2ef6fea40df0289f1a38d` on `feat/keyword-v11-p1-p3`.
- Do not merge, deploy, or modify Production data.
- Reuse the existing keyword subsystem; do not create a parallel implementation.
- Every behavior change follows a witnessed RED/GREEN cycle.
- All writes require server-side validation, existing RBAC/CSRF, project isolation, and auditability.
- Missing evidence must stay unknown and must never be fabricated.

---

### Task 1: P1 lifecycle, validation, bulk create, and filters

**Files:**
- Modify: `prisma/models/keyword-demand.prisma`
- Create: `prisma/migrations/20260902090000_add_keyword_v11_p1_p3/migration.sql`
- Create: `src/modules/keywords/keyword.schema.ts`
- Modify: `src/modules/keywords/keyword.types.ts`
- Modify: `src/modules/keywords/keyword.repository.ts`
- Modify: `src/modules/keywords/keyword.service.ts`
- Modify: `src/modules/keywords/keyword.routes.ts`
- Modify: `src/modules/keywords/keyword.web.routes.ts`
- Modify: `src/modules/keywords/keyword.web.repository.ts`
- Modify: `src/views/keywords/index.ejs`
- Modify: `src/public/css/p11-keywords.css`
- Test: `tests/unit/keyword-schema.test.ts`
- Test: `tests/integration/keywords.service.test.ts`
- Test: `tests/integration/keywords.api.test.ts`
- Test: `tests/integration/keywords.web.test.ts`

**Interfaces:**
- Produces: `keywordListQuerySchema`, `keywordCreateSchema`, `keywordBulkCreateSchema`, `keywordUpdateSchema`.
- Produces: `KeywordService.createManualBulk()` returning `{created, duplicates, invalid}` and filtered `list(projectId, filters)`.

- [ ] Write unit/integration tests proving invalid enum/body rejection, deterministic per-line bulk results, project-scoped dedupe, lifecycle backfill semantics, and combined filters.
- [ ] Run the new tests and confirm failures are caused by missing schemas/methods/columns.
- [ ] Add the lifecycle enum/column and additive migration with explicit backfill.
- [ ] Implement Zod request schemas, repository filters, bulk service transaction semantics, and API/web routes.
- [ ] Add multiline creation, lifecycle control, and filter controls to the current workbench.
- [ ] Run P1 tests, Prisma validation/generation, typecheck, full Vitest, and build.
- [ ] Commit P1 with tests and migration.

### Task 2: P2 Cluster upgrade

**Files:**
- Modify: `prisma/models/keyword-demand.prisma`
- Modify: `prisma/migrations/20260902090000_add_keyword_v11_p1_p3/migration.sql`
- Modify: `src/modules/keywords/keyword.schema.ts`
- Modify: `src/modules/keywords/keyword.repository.ts`
- Modify: `src/modules/keywords/keyword.service.ts`
- Modify: `src/modules/keywords/keyword.routes.ts`
- Modify: `src/modules/keywords/keyword.web.routes.ts`
- Modify: `src/modules/keywords/keyword.web.repository.ts`
- Modify: `src/views/keywords/index.ejs`
- Test: `tests/integration/keywords.repository.test.ts`
- Test: `tests/integration/keywords.service.test.ts`
- Test: `tests/integration/keywords.api.test.ts`
- Test: `tests/integration/keywords.web.test.ts`

**Interfaces:**
- Produces: `renameGroup`, `setGroupPrimaryKeyword`, and `assignKeywordsToGroup` service commands.
- Invariant: primary keyword is project-local and is always a member of its Cluster.

- [ ] Write failing tests for rename conflicts, foreign-project primary rejection, automatic primary membership, and atomic bulk assignment.
- [ ] Run them and verify expected RED failures.
- [ ] Add nullable `primaryKeywordId`, foreign key/index, repository commands, transactional service invariants, routes, and audit events.
- [ ] Upgrade UI copy and controls from generic groups to Keyword Cluster while preserving stored rows.
- [ ] Run P2 targeted tests plus P1 regression tests, Prisma validation/generation, typecheck, full Vitest, and build.
- [ ] Commit P2 independently.

### Task 3: P3 explainable opportunity score

**Files:**
- Modify: `prisma/models/keyword-demand.prisma`
- Modify: `prisma/migrations/20260902090000_add_keyword_v11_p1_p3/migration.sql`
- Create: `src/modules/keywords/keyword-opportunity-score.ts`
- Create: `src/modules/keywords/keyword-opportunity.repository.ts`
- Create: `src/modules/keywords/keyword-opportunity.service.ts`
- Modify: `src/modules/keywords/keyword.schema.ts`
- Modify: `src/modules/keywords/keyword.routes.ts`
- Modify: `src/modules/keywords/keyword.web.routes.ts`
- Modify: `src/modules/keywords/keyword.web.repository.ts`
- Modify: `src/views/keywords/index.ejs`
- Modify: `src/public/css/p11-keywords.css`
- Test: `tests/unit/keyword-opportunity-score.test.ts`
- Test: `tests/integration/keyword-opportunity.service.test.ts`
- Test: `tests/integration/keywords.api.test.ts`
- Test: `tests/integration/keywords.web.test.ts`
- Test: `tests/e2e/keywords.spec.ts`

**Interfaces:**
- Produces: `scoreKeywordOpportunity(input): KeywordOpportunityScoreResult` with nullable score, `dataConfidence`, component breakdown, formula version, and source provenance.
- Produces: `KeywordOpportunityService.calculate(projectId, keywordId, actorUserId)` and latest snapshot projection.

- [ ] Write literal table-driven RED tests for full, partial, insufficient, and unknown evidence; ensure missing demand/difficulty/rank cannot become invented values.
- [ ] Implement the minimal pure scorer and confirm GREEN.
- [ ] Write failing persistence/API/UI tests for append-only snapshots, project isolation, RBAC/CSRF, latest-score display, and breakdown/confidence labels.
- [ ] Add the snapshot model/migration, repository/service, calculation routes, and workbench score presentation.
- [ ] Run all P1-P3 targeted tests, Prisma validation/generation, typecheck, full Vitest, build, and E2E keyword smoke tests.
- [ ] Commit P3 independently.

### Task 4: Exact-head CI and Production comparison report

**Files:**
- Create: `docs/development/keywords-v11-p1-p3-production-comparison.md`

**Interfaces:**
- Consumes: exact branch HEAD and all P1-P3 verification outputs.
- Produces: auditable completed-capability, files, migration, API, UI, test, CI, rollback, and P4-P12 remainder report.

- [ ] Re-read the V1.1 P1-P3 and Definition of Done checklist against implemented behavior.
- [ ] Run fresh full Prisma validation/generation, typecheck, Vitest, build, and E2E verification.
- [ ] Record exact HEAD and clean diff/status; push only the feature branch.
- [ ] Wait for exact-head `verify`, `e2e`, `production-audit`, and `deployment-artifact` checks.
- [ ] Write the Production comparison report with exact evidence and remaining P4-P12 scope.
- [ ] Commit and push the report, then verify the report commit's exact-head CI before handoff.
